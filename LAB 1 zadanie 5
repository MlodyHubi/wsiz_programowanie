# Pobranie trzech liczb od użytkownika
x = float(input("Podaj liczbę x: "))
y = float(input("Podaj liczbę y: "))
z = float(input("Podaj liczbę z: "))

# Porządkowanie liczb
if x > y:
    x, y = y, x
if x > z:
    x, z = z, x
if y > z:
    y, z = z, y

# Wyświetlenie posortowanych liczb
print(f"Liczby po posortowaniu: {x}, {y}, {z}")
